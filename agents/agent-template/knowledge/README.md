# Knowledge Store

Add your agent's knowledge files here. Use markdown (`.md`) for human-readable context and JSON (`.json`) for structured data.

## Guidelines

- One file per topic or domain area.
- Use clear headings and structured content.
- Update this README when adding new files.
